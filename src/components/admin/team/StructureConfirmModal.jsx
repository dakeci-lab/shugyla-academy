import ConfirmRoleActionModal from './ConfirmRoleActionModal'

/** Shared confirmation modal for structure archive/restore/dirty navigation. */
export default function StructureConfirmModal(props) {
  return <ConfirmRoleActionModal {...props} />
}
